# Expo SDK Version

This project uses Expo SDK 54. Read the versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

The project was downgraded from SDK 56 to match the user's Expo Go app.
